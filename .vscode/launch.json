{
  "version": "0.2.0",
  "configurations": [{
    "name": "Python: Run current file",
    "type": "debugpy",
    "request": "launch",
    "program": "${file}",
    "console": "integratedTerminal",
    "cwd": "${workspaceFolder}",
    "justMyCode": true
  }]
}
